/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {Coords} from './coords';

describe('Coords', () => {
  it('should create an instance', () => {
    expect(new Coords()).toBeTruthy();
  });
});
