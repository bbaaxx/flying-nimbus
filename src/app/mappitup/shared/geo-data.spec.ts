/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {GeoData} from './geo-data';

describe('GeoData', () => {
  it('should create an instance', () => {
    expect(new GeoData()).toBeTruthy();
  });
});
